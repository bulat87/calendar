h1 My events
.container
  table.table.table-striped.table-bordered.table-hover
    thead
      tr
      th #
      th Event Title
      th Date
      
      tbody
        - @events.each do |event|
          tr
          td= event.id
          td= event.title
          td= event.event_start 